{
  "name": "adaptive-tutor-mvp",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm --filter @app/api test",
    "audit:gate": "node scripts/ci/audit-gate.mjs"
  }
}
