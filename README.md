# Adaptive Tutor MVP

Monorepo:
- `apps/api`: NestJS + Prisma + BullMQ
- `apps/web`: React + Vite + Tailwind
- `packages/shared`: shared zod schemas/types
- `infra/docker`: postgres + redis

See run instructions in the response output.
